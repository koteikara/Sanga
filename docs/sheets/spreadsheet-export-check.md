# スプレッドシートCSV検証レポート

このレポートは、Googleスプレッドシートの「試合」シートから出力したCSV相当の内容を、公開JSONへ反映する前にリポジトリ側で確認した結果です。前回確認した `docs/sheets/schedule.initial.csv` を、2026-06-19に公開JSON `public/data/matches.json` へ反映しました。

運用時の標準手順は `docs/operation-flow.md` に集約しています。スプレッドシート更新後のCSV出力、JSON生成、検証、GitHub Pages確認、本番デプロイ、本番確認は同手順を参照してください。

## 確認対象

| 項目 | 内容 |
| --- | --- |
| 確認対象CSV | `docs/sheets/schedule.initial.csv` |
| 確認日 | 2026-06-19 |
| 用途 | Googleスプレッドシート「試合」シートの初期取り込み用CSV |
| 公開JSONへの反映 | 2026-06-19に `public/data/matches.json` へ反映済み |
| 生成JSON候補 | `tmp/matches.generated.json`（一時生成物のためコミット対象外） |

## CSV構造の確認結果

| 確認観点 | 結果 | メモ |
| --- | --- | --- |
| 列数 | 問題なし | ヘッダーは拡張後の31列です。 |
| 行数 | 問題なし | ヘッダー1行 + 試合38行です。 |
| ID | 問題なし | `sec01` 〜 `sec38` が連番で存在します。 |
| 表示順 | 問題なし | 1〜38が重複なく並んでいます。 |
| 大会 | 問題なし | 全件 `J1` です。 |
| 大会表示 | 問題なし | 全件 `明治安田J1リーグ` です。 |
| 表示フラグ | 問題なし | 全件 `表示` です。 |
| メモ | 問題なし | 全件空欄です。公開JSONへ出さない方針を維持します。 |
| `sec16` の開催年 | 問題なし | `開催年` は `2026` です。開催日・候補日は空欄、注記 `※1` ありです。 |

## 日付・候補日の確認結果

| 確認観点 | 結果 | メモ |
| --- | --- | --- |
| 開催日の日付形式 | 問題なし | 入力済みの開催日は `YYYY-MM-DD` 形式です。 |
| 候補日の日付形式 | 問題なし | 候補日が入力されている場合は `YYYY-MM-DD` 形式、複数日は `|` 区切りで扱います。 |
| 開催日と候補日の同時入力 | 問題なし | 同時入力されている行はありません。 |
| 未確定日程の扱い | 問題なし | 開催日・候補日が空欄の行は、公開前に状態・注記・根拠URLを合わせて確認します。 |

## 注記番号と注記本文の確認結果

注記番号が入っている行では、注記本文の先頭番号と対応していることを確認しました。

| ID | 注記番号 | 確認結果 |
| --- | --- | --- |
| `sec16` | `※1` | 問題なし |
| `sec25` | `※2` | 問題なし |
| `sec26` | `※3` | 問題なし |
| `sec31` | `※4` | 問題なし |
| `sec32` | `※5` | 問題なし |
| `sec33` | `※6` | 問題なし |
| `sec34` | `※7` | 問題なし |

## CSVから生成したJSON候補の検証結果

リポジトリ直下で以下を実行し、CSVから確認用JSONを生成しました。

```bash
node tools/generate-matches-from-csv.js docs/sheets/schedule.initial.csv tmp/matches.generated.json
```

生成結果:

* `tmp/matches.generated.json` を生成しました。
* 生成件数は38件です。
* ID重複、必須項目、日付形式の簡易チェックに成功しました。

続けて、生成JSON候補を38件想定で検証しました。

```bash
node tools/validate-generated-matches.js tmp/matches.generated.json --expected-count 38
```

検証結果:

* 生成JSONの検証に成功しました。
* 件数は38件です。
* 警告は0件です。
* エラーは0件です。

## 現在の公開JSONとの差分確認

`tmp/matches.generated.json` と `public/data/matches.json` を比較し、公開画面が参照している主要項目が欠落・破損していない見込みであることを確認しました。

| 確認観点 | 結果 | メモ |
| --- | --- | --- |
| 試合件数 | 問題なし | どちらも38件です。 |
| ID | 問題なし | どちらも `sec01` 〜 `sec38` で一致しています。 |
| 節 | 問題なし | 主要項目比較で差分はありません。 |
| 対戦相手 | 問題なし | 主要項目比較で差分はありません。 |
| HOME/AWAY | 問題なし | `home_away`、`home_away_label` とも主要項目比較で差分はありません。 |
| 会場 | 問題なし | 主要項目比較で差分はありません。 |
| 開催日 | 問題なし | 主要項目比較で差分はありません。 |
| 候補日 | 問題なし | 主要項目比較で差分はありません。 |
| 注記 | 問題なし | 主要項目比較で差分はありません。 |
| 根拠URL | 問題なし | 主要項目比較で差分はありません。 |
| 既存画面参照項目 | 問題なし | 現在の公開JSONにあるキーの欠落はありません。 |
| 追加項目 | 想定内 | 生成JSON候補には `match_status`、`is_visible` が追加されます。 |

## 公開JSONへ反映する前に確認すべき注意点

* 2026-06-19に `public/data/matches.json` へ反映済みです。以後もCSVから公開JSONへ反映する場合は、生成後に差分と検証結果を確認します。
* 生成JSON候補には `match_status`、`is_visible` などの拡張項目が追加されるため、公開画面側が未知の項目を無視できることを確認します。
* `メモ` 列は運用者向けの非公開メモです。公開JSONへ出力されていないことを継続確認します。
* `sec16` は開催年 `2026`、開催日・候補日空欄、注記 `※1` ありの未確定日程として扱います。公式発表後に開催日または候補日を更新する場合は、開催日と候補日を同時入力しないよう確認します。
* 公開JSONへ反映するPRでは、生成後に `node tools/validate-matches.js` を実行し、既存画面の表示・フィルタ・LocalStorage関連機能が破綻しないことを確認します。
* `tmp/` 配下の生成物は一時ファイルのためコミットしません。
